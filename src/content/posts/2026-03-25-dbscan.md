---
title: "DBSCAN"
description: "Density-based clustering: form clusters from regions of high point density, label sparse points as noise. Handles arbitrary cluster shapes; no k to specify."
date: "2026-03-25"
draft: false
tags: ["concepts"]
category: "concepts"
---

## Summary

**DBSCAN** (Density-Based Spatial Clustering of Applications with Noise; [Ester et al., 1996](https://www.dbs.ifi.lmu.de/Publikationen/Papers/KDD-96.final.frame.pdf)) groups points by local density. A point with at least $\text{minPts}$ neighbors within distance $\varepsilon$ is a **core point**. Connected core points and their nearby points form clusters; all other points are **noise**.

Unlike k-means, DBSCAN:

- **Discovers the number of clusters automatically**. No $k$ required.
- **Handles arbitrary cluster shapes** (concentric rings, elongated curves, anything connected).
- **Identifies noise / outliers** as a first-class concept.

Used in: anomaly detection, geospatial clustering (Uber, taxi data), molecule grouping, social network community detection.

## The algorithm

Two parameters: $\varepsilon$ (neighborhood radius) and $\text{minPts}$ (minimum points, including the point itself, to form a dense region).

For each unvisited point $p$:

1. Find $N_\varepsilon(p)$ = all points within distance $\varepsilon$.
2. If $|N_\varepsilon(p)| < \text{minPts}$, label $p$ as noise (may later be reclassified as part of another cluster).
3. Otherwise, $p$ is a **core point**. Start a new cluster:
   - Add $p$ and all points in $N_\varepsilon(p)$ to the cluster.
   - For each new core point in the cluster, expand by adding *its* $\varepsilon$-neighbors.
   - Continue until no more points can be added.

Each non-noise point ends up in exactly one cluster.

## Three types of points

- **Core**: its $\varepsilon$-neighborhood contains $\ge \text{minPts}$ points, counting the point itself.
- **Border**: fewer than $\text{minPts}$ neighbors but within $\varepsilon$ of a core point.
- **Noise**: neither. Sparsely located, far from any dense region.

**Learning objective:** trace how overlapping neighborhoods of core points expand one cluster, while a nearby non-core point becomes border and an isolated point remains noise.

<!-- visual:dbscan-neighborhood-expansion -->
<figure class="learning-figure plot-panel" aria-labelledby="dbscan-neighborhood-title">
	<p class="visual-kicker">Local density becomes a cluster</p>
	<p class="visual-title" id="dbscan-neighborhood-title">Which points let DBSCAN keep expanding?</p>
	<svg viewBox="0 0 360 300" role="img" aria-labelledby="dbscan-svg-title dbscan-svg-desc">
		<title id="dbscan-svg-title">DBSCAN neighborhood expansion through two core points</title>
		<desc id="dbscan-svg-desc">With minimum points equal to four and each point counting itself, core point A has four points in its dashed epsilon-neighborhood. Core point B also has at least four. Because A and B lie within each other's neighborhoods, expansion continues through both cores. A diamond lies within epsilon of B but has fewer than four points in its own neighborhood, so it is a border point and does not expand the cluster. An x lies outside every core neighborhood and remains noise.</desc>
		<rect class="viz-plot-bg" x="18" y="36" width="324" height="210" rx="4"></rect>
		<text class="viz-axis-label" x="28" y="58">MINPTS = 4 (THE CENTER POINT COUNTS)</text>
		<circle cx="110" cy="140" r="72" style="fill:none;stroke:var(--viz-focus-stroke);stroke-width:1.7;stroke-dasharray:5 4"></circle>
		<circle cx="170" cy="140" r="72" style="fill:none;stroke:var(--viz-input-stroke);stroke-width:1.7;stroke-dasharray:5 4"></circle>
		<text class="viz-callout" x="63" y="78">A's ε-neighborhood</text>
		<text class="viz-callout" x="176" y="78">B's ε-neighborhood</text>
		<circle class="viz-operating-point" cx="75" cy="110" r="5"></circle>
		<circle class="viz-operating-point" cx="80" cy="170" r="5"></circle>
		<circle class="viz-operating-point" cx="180" cy="84" r="5"></circle>
		<circle class="viz-operating-point" cx="205" cy="165" r="5"></circle>
		<rect class="viz-node viz-node--focus" x="103" y="133" width="14" height="14" rx="1"></rect>
		<text class="viz-callout" x="92" y="130" text-anchor="end">core A</text>
		<rect class="viz-node viz-node--input" x="163" y="133" width="14" height="14" rx="1"></rect>
		<text class="viz-callout" x="178" y="130">core B</text>
		<path class="viz-node viz-node--output" d="M235 133L242 140L235 147L228 140Z"></path>
		<text class="viz-callout" x="246" y="144">border</text>
		<path d="M305 104L319 118M319 104L305 118" style="fill:none;stroke:var(--viz-warning-stroke);stroke-width:2.5"></path>
		<text class="viz-callout" x="312" y="132" text-anchor="middle">noise</text>
		<path d="M121 197C137 208 153 208 169 197" style="fill:none;stroke:var(--viz-edge);stroke-width:1.8"></path>
		<text class="viz-label" x="145" y="222" text-anchor="middle">overlap joins A and B</text>
		<text class="viz-label" x="28" y="238">Only squares (core points) continue the expansion.</text>
		<rect class="viz-node viz-node--focus" x="28" y="263" width="10" height="10" rx="1"></rect>
		<text class="viz-label" x="44" y="272">core: expand</text>
		<path class="viz-node viz-node--output" d="M151 262L157 268L151 274L145 268Z"></path>
		<text class="viz-label" x="163" y="272">border: include, stop</text>
		<path d="M290 263L300 273M300 263L290 273" style="fill:none;stroke:var(--viz-warning-stroke);stroke-width:2"></path>
		<text class="viz-label" x="306" y="272">noise</text>
	</svg>
	<figcaption><strong>Read it this way:</strong> start at square A and count its dashed circle, including A itself. It is core, so DBSCAN adds its neighbors and tests them. Square B is also core, so its overlapping circle extends the cluster to the right. The diamond is close enough to join as a border point but cannot extend the cluster; the × is outside every core neighborhood and remains noise. Original schematic checked against <a href="https://cdn.aaai.org/KDD/1996/KDD96-037.pdf">Ester et al. (1996)</a> and the <a href="https://scikit-learn.org/stable/modules/generated/sklearn.cluster.DBSCAN.html">scikit-learn DBSCAN documentation</a>.</figcaption>
</figure>

## Choosing $\varepsilon$ and $\text{minPts}$

Heuristics:

- **$\text{minPts}$**: rule of thumb $\text{minPts} \ge d + 1$ where $d$ is the data dimensionality; often $\text{minPts} = 4$ for 2D, $2d$ for higher.
- **$\varepsilon$**: plot the sorted distances to the $\text{minPts}$-th nearest neighbor for each point. Pick $\varepsilon$ at the "knee" of the curve. Below it the region is dense, above it sparse.

Choosing these badly produces either one giant cluster (too large $\varepsilon$) or all noise (too small $\varepsilon$).

## Strengths and weaknesses

**Strengths:**

- No $k$ to specify.
- Arbitrary cluster shapes.
- Robust to outliers (gets noise labels).
- Deterministic given parameters and order of points (mostly).

**Weaknesses:**

- **One $\varepsilon$ for all clusters**: fails when clusters have very different densities.
- **Curse of dimensionality**: in high-d, all points become roughly equidistant, so $\varepsilon$ becomes meaningless. Use HDBSCAN or alternative.
- **Compute**: naive $O(n^2)$ for distance computation; $O(n \log n)$ with a spatial index (KD-tree, ball tree) in low dimensions.

## Variants

- **HDBSCAN** [(Campello et al., 2013)](https://link.springer.com/chapter/10.1007/978-3-642-37456-2_14): extends DBSCAN to varying densities by building a hierarchy and extracting clusters from a stability-based selection. Often the better default in practice.
- **OPTICS**: similar idea, produces a reachability plot rather than discrete clusters.

## When to use

| Setting | DBSCAN vs. alternative |
|---------|----------------------|
| Geospatial clusters | DBSCAN (or HDBSCAN) |
| Outlier detection | DBSCAN gets it for free |
| Convex, similar-density clusters | k-means simpler |
| High-dimensional data | DBSCAN struggles; use embedding + clustering |
| Need cluster probability | GMM or HDBSCAN |

## Common pitfalls

- **Picking $\varepsilon$ without the k-distance plot.** Almost always wrong by orders of magnitude.
- **Running on raw high-dim data.** Reduce dimension first (PCA, UMAP, learned embedding).
- **Comparing DBSCAN and k-means on accuracy.** They optimize different objectives; compare on what you actually care about (e.g., user-validated cluster purity).
- **Re-running DBSCAN with the same parameters on different-density datasets.** $\varepsilon$ doesn't transfer across datasets without normalization.
