{
  "name": "mlmentorship",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Senior ML interview prep",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "npm run check:style && npm run check:site && npm run check:visuals && astro build && node scripts/check-visual-coverage.mjs --dist && node scripts/check-visual-review.mjs --dist && pagefind --site dist && node scripts/check-links.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check && npm run check:style && npm run check:site && npm run check:visuals",
    "check:links": "node scripts/check-links.mjs",
    "check:style": "node scripts/check-editorial-style.mjs",
    "check:site": "node scripts/check-site-contracts.mjs",
    "check:visuals": "node scripts/check-visual-coverage.mjs && node scripts/check-coding-visual-architecture.mjs && node scripts/check-coding-visuals.mjs && node scripts/check-coding-visual-interactions.mjs",
    "visuals:status": "node scripts/check-visual-coverage.mjs --next=20",
    "visuals:batch": "node scripts/create-visual-batch.mjs",
    "content:coding-book": "node scripts/generate-coding-question-book.mjs --force"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.14",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^5.18.1",
    "katex": "^0.16.11",
    "mermaid": "^11.14.0",
    "reading-time": "^1.5.0",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@resvg/resvg-js": "^2.6.2",
    "astro-mermaid": "^2.0.1",
    "pagefind": "^1.5.2",
    "satori": "^0.26.0",
    "typescript": "^5.6.3"
  }
}
