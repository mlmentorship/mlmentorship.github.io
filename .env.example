# Build-time public feature flags. Omit the variable or set true to expose prep.
# Set false to hide Prep navigation, homepage/Start Here entry points, and
# Practice Mode; /prep/* and legacy prep routes redirect to /questions/.
PUBLIC_PREP_TOOLS=true